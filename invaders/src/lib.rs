pub mod frame;          //frame logic module for every frame that we render  
pub mod player;         //player module
pub mod render;         //module for rendering our frame to terminal

//constants for use
pub const NUM_ROWS: usize = 20;         //num of rows we are goin to have in our playing field
pub const NUM_COLS: usize = 40;         //num of columns


//module for rendering our frame to terminal